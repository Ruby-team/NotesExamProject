
<%= form_for([@activity, @activity.filees.build], html: { multipart: true }) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :attachment %>
    <%= f.file_field :attachment %>
    <p>
      <%= f.submit %>
    </p>
<% end %>